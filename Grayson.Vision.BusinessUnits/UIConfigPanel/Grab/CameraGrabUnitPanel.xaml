<UserControl x:Class="Grayson.Vision.BusinessUnits.UIConfigPanel.Grab.CameraGrabUnitPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="480" Height="200" Margin="12">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">绑定相机DeviceKey：</TextBlock>
        <ComboBox x:Name="CbxCameraDeviceKey" Grid.Row="0" Grid.Column="1" IsEditable="False"/>

        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">采集超时(毫秒)：</TextBlock>
        <TextBox x:Name="TxtGrabTimeout" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"/>

        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center">启用当前单元：</TextBlock>
        <CheckBox x:Name="ChkUnitEnable" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Content="开启"/>

        <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Background="#f5f5f5" Padding="8">
            <StackPanel>
                <TextBlock Foreground="#666" FontSize="12">【输入】无前置依赖</TextBlock>
                <TextBlock Foreground="#666" FontSize="12">【输出】Grab.SourceImage（原图）、Grab.TriggerTime（时间戳）</TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>