<UserControl x:Class="Grayson.Vision.Nodes.All.DataStorage.SaveImage.SaveImageTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Grayson.Vision.Nodes.All.DataStorage.SaveImage"
             mc:Ignorable="d" 
             d:DesignHeight="260" d:DesignWidth="300"
             Background="#1E1E1E">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Grayson.Vision.Nodes;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type local:SaveImageParam}">
                <StackPanel Margin="10,6">
                    <Border Background="#252526" BorderBrush="#333337" BorderThickness="1" CornerRadius="4" Padding="8">
                        <StackPanel>
                            <TextBlock Text="保存根目录 (Base Directory)" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding BasePath, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource NodeParamInputStyle}" Margin="0,0,0,8"/>

                            <TextBlock Text="图片保存格式" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                            <ComboBox SelectedValue="{Binding ImageFormat, UpdateSourceTrigger=PropertyChanged}"
                                      SelectedValuePath="Tag"
                                      Style="{StaticResource NodeParamComboBoxStyle}" Margin="0,0,0,8">
                                <ComboBoxItem Content="JPG 格式 (压缩快体积小)" Tag="jpg"/>
                                <ComboBoxItem Content="PNG 格式 (无损压缩)" Tag="png"/>
                                <ComboBoxItem Content="BMP 格式 (无损未压缩)" Tag="bmp"/>
                            </ComboBox>

                            <UniformGrid Columns="2" Margin="0,4,0,0">
                                <CheckBox Content="存 OK 图" IsChecked="{Binding SaveOkImage, Mode=TwoWay}" Foreground="#DDD"/>
                                <CheckBox Content="存 NG 图" IsChecked="{Binding SaveNgImage, Mode=TwoWay}" Foreground="#DDD"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid d:DataContext="{d:DesignInstance Type=local:SaveImageParam, IsDesignTimeCreatable=True}">
        <ContentControl Content="{Binding}" Focusable="False" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
    </Grid>
</UserControl>