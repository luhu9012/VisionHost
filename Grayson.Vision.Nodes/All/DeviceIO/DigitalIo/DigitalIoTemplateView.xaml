<UserControl x:Class="Grayson.Vision.Nodes.All.DeviceIO.DigitalIo.DigitalIoTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Grayson.Vision.Nodes.All.DeviceIO.DigitalIo"
             mc:Ignorable="d"
             d:DesignHeight="280" d:DesignWidth="300"
             Background="#1E1E1E">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Grayson.Vision.Nodes;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVis"/>

            <DataTemplate DataType="{x:Type local:DigitalIoParam}">
                <StackPanel Margin="10,6">
                    <Border Background="#252526" BorderBrush="#333337" BorderThickness="1" CornerRadius="4" Padding="8">
                        <StackPanel>
                            <!-- 控制卡别名绑定 -->
                            <TextBlock Text="绑定板卡设备别名" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding CardAlias, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource NodeParamInputStyle}" Margin="0,0,0,8"/>

                            <!-- 模式选择：输入/输出 -->
                            <CheckBox Content="切换为输出模式 (Output Mode)" 
                                      IsChecked="{Binding IsOutput, Mode=TwoWay}" 
                                      Foreground="#DDD" Margin="0,0,0,8"/>

                            <!-- 引脚编号 -->
                            <TextBlock Text="IO 引脚索引号 (IoIndex)" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding IoIndex, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource NodeParamInputStyle}" Margin="0,0,0,8"/>

                            <!-- 仅在输出模式下展示默认输出状态 -->
                            <StackPanel Visibility="{Binding IsOutput, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="默认输出状态" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                                <CheckBox Content="设为高电平/ON" 
                                          IsChecked="{Binding OutState, Mode=TwoWay}" 
                                          Foreground="#DDD" Margin="0,0,0,4"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid d:DataContext="{d:DesignInstance Type=local:DigitalIoParam, IsDesignTimeCreatable=True}">
        <ContentControl Content="{Binding}" Focusable="False" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
    </Grid>
</UserControl>