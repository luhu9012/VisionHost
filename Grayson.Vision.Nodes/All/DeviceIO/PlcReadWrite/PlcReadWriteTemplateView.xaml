<UserControl x:Class="Grayson.Vision.Nodes.All.DeviceIO.PlcReadWrite.PlcReadWriteTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Grayson.Vision.Nodes.All.DeviceIO.PlcReadWrite"
             mc:Ignorable="d"
             d:DesignHeight="280" d:DesignWidth="300"
             Background="#1E1E1E">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Grayson.Vision.Nodes;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVis"/>

            <DataTemplate DataType="{x:Type local:PlcReadWriteParam}">
                <StackPanel Margin="10,6">
                    <Border Background="#252526" BorderBrush="#333337" BorderThickness="1" CornerRadius="4" Padding="8">
                        <StackPanel>
                            <TextBlock Text="绑定 PLC 设备别名" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding PlcAlias, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource NodeParamInputStyle}" Margin="0,0,0,8"/>

                            <CheckBox Content="切换为写入模式 (Write Mode)" 
                                      IsChecked="{Binding IsWriteMode, Mode=TwoWay}" 
                                      Foreground="#DDD" Margin="0,0,0,8"/>

                            <TextBlock Text="PLC 寄存器地址 (Address)" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource NodeParamInputStyle}" Margin="0,0,0,8"/>

                            <TextBlock Text="数据类型 (DataType)" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                            <ComboBox SelectedValue="{Binding DataType, UpdateSourceTrigger=PropertyChanged}"
                                      SelectedValuePath="Tag"
                                      Style="{StaticResource NodeParamComboBoxStyle}" Margin="0,0,0,8">
                                <ComboBoxItem Content="Bool (位)" Tag="Bool"/>
                                <ComboBoxItem Content="Int16 (短整型)" Tag="Int16"/>
                                <ComboBoxItem Content="Int32 (整型)" Tag="Int32"/>
                                <ComboBoxItem Content="Float (单精度浮点)" Tag="Float"/>
                                <ComboBoxItem Content="Double (双精度浮点)" Tag="Double"/>
                                <ComboBoxItem Content="String (字符串)" Tag="String"/>
                            </ComboBox>

                            <!-- 仅写入模式下展示默认写入设定值 -->
                            <StackPanel Visibility="{Binding IsWriteMode, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="写入设定值 (WriteValue)" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding WriteValue, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource NodeParamInputStyle}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid d:DataContext="{d:DesignInstance Type=local:PlcReadWriteParam, IsDesignTimeCreatable=True}">
        <ContentControl Content="{Binding}" Focusable="False" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
    </Grid>
</UserControl>