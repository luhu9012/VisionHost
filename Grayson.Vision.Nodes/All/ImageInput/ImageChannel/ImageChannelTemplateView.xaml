<UserControl x:Class="Grayson.Vision.Nodes.All.ImageInput.ImageChannel.ImageChannelTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Grayson.Vision.Nodes.All.ImageInput.ImageChannel"
             mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="320" Background="#1E1E1E">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Grayson.Vision.Nodes;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type local:ImageChannelParam}">
                <StackPanel Margin="10,6">

                    <!-- 色彩空间与提取通道配置 -->
                    <Border Background="#252526" BorderBrush="#333337" BorderThickness="1" CornerRadius="4" Padding="8">
                        <StackPanel>
                            <TextBlock Text="色彩空间" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                            <ComboBox Style="{StaticResource NodeParamComboBoxStyle}"
                                      SelectedIndex="{Binding ColorSpace, Mode=TwoWay, Converter={StaticResource EnumToIntConverter}}"
                                      Margin="0,0,0,8">
                                <ComboBoxItem Content="🔴 RGB 空间"/>
                                <ComboBoxItem Content="🌈 HSV 空间"/>
                            </ComboBox>

                            <TextBlock Text="提取通道" Style="{StaticResource NodeParamHeaderLabel}" Margin="0,0,0,4"/>
                            <ComboBox Style="{StaticResource NodeParamComboBoxStyle}"
                                      SelectedIndex="{Binding SelectedChannelIndex, Mode=TwoWay}">
                                <ComboBoxItem Content="通道 1 ( R / H )"/>
                                <ComboBoxItem Content="通道 2 ( G / S )"/>
                                <ComboBoxItem Content="通道 3 ( B / V )"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid d:DataContext="{d:DesignInstance Type=local:ImageChannelParam, IsDesignTimeCreatable=True}">
        <ContentControl Content="{Binding}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
    </Grid>
</UserControl>