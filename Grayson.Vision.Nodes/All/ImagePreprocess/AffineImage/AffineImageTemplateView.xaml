<UserControl x:Class="Grayson.Vision.Nodes.All.ImagePreprocess.AffineImage.AffineImageTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Grayson.Vision.Nodes.All.ImagePreprocess.AffineImage"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300" Background="#1E1E1E">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Grayson.Vision.Nodes;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type local:AffineImageParam}">
                <StackPanel Margin="10">
                    <TextBlock Text="旋转中心 Row (Y):" Style="{StaticResource NodeParamHeaderLabel}"/>
                    <TextBox Text="{Binding CenterRow, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NodeParamInputStyle}" Margin="0 0 0 8"/>

                    <TextBlock Text="旋转中心 Col (X):" Style="{StaticResource NodeParamHeaderLabel}"/>
                    <TextBox Text="{Binding CenterCol, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NodeParamInputStyle}" Margin="0 0 0 8"/>

                    <TextBlock Text="旋转角度 (Degree):" Style="{StaticResource NodeParamHeaderLabel}"/>
                    <TextBox Text="{Binding AngleDegree, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NodeParamInputStyle}"/>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid d:DataContext="{d:DesignInstance Type=local:AffineImageParam, IsDesignTimeCreatable=True}">
        <ContentControl Content="{Binding}"/>
    </Grid>
</UserControl>