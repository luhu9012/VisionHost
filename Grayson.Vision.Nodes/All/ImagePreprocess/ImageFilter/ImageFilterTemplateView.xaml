<UserControl x:Class="Grayson.Vision.Nodes.All.ImagePreprocess.ImageFilter.ImageFilterTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Grayson.Vision.Nodes.All.ImagePreprocess.ImageFilter"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300" Background="#1E1E1E">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Grayson.Vision.Nodes;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type local:ImageFilterParam}">
                <StackPanel Margin="10">
                    <TextBlock Text="滤波/形态学算子类型:" Style="{StaticResource NodeParamHeaderLabel}"/>
                    <ComboBox SelectedValue="{Binding Method}" Style="{StaticResource NodeParamComboBoxStyle}" Margin="0 0 0 10">
                        <local:FilterMethod>Gauss</local:FilterMethod>
                        <local:FilterMethod>Median</local:FilterMethod>
                        <local:FilterMethod>Erode</local:FilterMethod>
                        <local:FilterMethod>Dilate</local:FilterMethod>
                        <local:FilterMethod>Open</local:FilterMethod>
                        <local:FilterMethod>Close</local:FilterMethod>
                    </ComboBox>

                    <TextBlock Text="滤波/形态学核大小 (Kernel Size):" Style="{StaticResource NodeParamHeaderLabel}"/>
                    <Grid Margin="0 0 0 10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <Slider Value="{Binding KernelSize, UpdateSourceTrigger=PropertyChanged}" Minimum="1" Maximum="31" TickFrequency="2" IsSnapToTickEnabled="True"/>
                        <TextBox Grid.Column="1" Text="{Binding KernelSize, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NodeParamInputStyle}" Margin="4 0 0 0"/>
                    </Grid>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid d:DataContext="{d:DesignInstance Type=local:ImageFilterParam, IsDesignTimeCreatable=True}">
        <ContentControl Content="{Binding}"/>
    </Grid>
</UserControl>