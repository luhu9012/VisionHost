<UserControl x:Class="Grayson.Vision.Nodes.All.ImagePreprocess.ImageThreshold.ImageThresholdTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Grayson.Vision.Nodes.All.ImagePreprocess.ImageThreshold"
             mc:Ignorable="d" d:DesignHeight="350" d:DesignWidth="300" Background="#1E1E1E">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Grayson.Vision.Nodes;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type local:ImageThresholdParam}">
                <StackPanel Margin="10">
                    <TextBlock Text="分割方式:" Style="{StaticResource NodeParamHeaderLabel}"/>
                    <ComboBox SelectedValue="{Binding Method}" Style="{StaticResource NodeParamComboBoxStyle}" Margin="0 0 0 10">
                        <local:ThresholdMethod>Fixed</local:ThresholdMethod>
                        <local:ThresholdMethod>Dynamic</local:ThresholdMethod>
                        <local:ThresholdMethod>Otsu</local:ThresholdMethod>
                    </ComboBox>

                    <!-- 固定阈值配置区 -->
                    <StackPanel Visibility="{Binding Method, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Fixed}">
                        <TextBlock Text="最小灰度 (Min Gray):" Style="{StaticResource NodeParamHeaderLabel}"/>
                        <Slider Value="{Binding MinGray}" Minimum="0" Maximum="255" Margin="0 0 0 5"/>
                        <TextBlock Text="最大灰度 (Max Gray):" Style="{StaticResource NodeParamHeaderLabel}"/>
                        <Slider Value="{Binding MaxGray}" Minimum="0" Maximum="255" Margin="0 0 0 10"/>
                    </StackPanel>

                    <!-- 动态阈值配置区 -->
                    <StackPanel Visibility="{Binding Method, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Dynamic}">
                        <TextBlock Text="窗口尺寸 (Mask Size):" Style="{StaticResource NodeParamHeaderLabel}"/>
                        <TextBox Text="{Binding DynamicMaskSize, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NodeParamInputStyle}" Margin="0 0 0 5"/>
                        <TextBlock Text="亮度偏移 (Offset):" Style="{StaticResource NodeParamHeaderLabel}"/>
                        <TextBox Text="{Binding DynamicOffset, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NodeParamInputStyle}" Margin="0 0 0 10"/>
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid d:DataContext="{d:DesignInstance Type=local:ImageThresholdParam, IsDesignTimeCreatable=True}">
        <ContentControl Content="{Binding}"/>
    </Grid>
</UserControl>