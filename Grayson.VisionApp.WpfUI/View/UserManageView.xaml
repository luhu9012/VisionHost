<UserControl x:Class="Grayson.Vision.WpfUI.View.UserManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:Grayson.Vision.WpfUI.Common.Converters"
             d:DesignHeight="850" d:DesignWidth="1200">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="NativeBoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题和操作按钮 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="👤 用户管理" Style="{StaticResource TitleTextStyle}" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="➕ 新增用户"
                        Command="{Binding AddUserCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Width="auto" Margin="5,0"/>
                <Button Content="📤 导出日志"
                        Command="{Binding ExportLogCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Width="auto" Margin="5,0"/>
            </StackPanel>
        </Grid>

        <!-- 统计卡片 -->
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="用户总数" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalUsers}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="管理员" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding AdminCount}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource ErrorBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="工程师" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding EngineerCount}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource WarningBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="操作员" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding OperatorCount}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 主内容区 -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320" x:Name="EditPanelColumn"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：用户列表 -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 搜索和筛选工具栏 -->
                <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,10,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="搜索:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0"/>
                        <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 Width="180" Margin="0,0,20,0"/>
                        <TextBlock Text="角色筛选:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0"/>
                        <ComboBox ItemsSource="{Binding RoleOptions}"
                                  SelectedItem="{Binding SelectedRoleFilter}"
                                  Style="{StaticResource ModernComboBoxStyle}"
                                  Width="120"/>
                    </StackPanel>
                </Border>

                <!-- 用户列表表格 -->
                <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,0,10,0">
                    <DataGrid ItemsSource="{Binding UserList}"
                              SelectedItem="{Binding SelectedUser}"
                              Style="{StaticResource ModernDataGridStyle}"
                              RowHeight="48">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="用户名" Binding="{Binding UserName}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="显示名称" Binding="{Binding DisplayName}" Width="130" IsReadOnly="True"/>
                            <DataGridTemplateColumn Header="角色" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="3" Padding="8,4" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Role}" Value="Administrator">
                                                            <Setter Property="Background" Value="#CC3300"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Role}" Value="Engineer">
                                                            <Setter Property="Background" Value="#FF8800"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Role}" Value="Operator">
                                                            <Setter Property="Background" Value="#0077CC"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding RoleDisplayName}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="状态" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock HorizontalAlignment="Center" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                                                            <Setter Property="Text" Value="✅ 启用"/>
                                                            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                                            <Setter Property="Text" Value="🚫 禁用"/>
                                                            <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="最后登录" Binding="{Binding LastLoginTime, StringFormat='MM-dd HH:mm'}" Width="110" IsReadOnly="True"/>
                            <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*" IsReadOnly="True"/>
                            <DataGridTemplateColumn Header="操作" Width="210">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="编辑"
                                                    Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    Padding="8,4" Margin="2,0" FontSize="12"/>
                                            <Button Content="重置密码"
                                                    Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    Padding="8,4" Margin="2,0" FontSize="12"/>
                                            <Button Content="删除"
                                                    Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource DangerButtonStyle}"
                                                    Padding="8,4" Margin="2,0" FontSize="12"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- 底部操作日志 -->
                <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Margin="0,10,10,0" MaxHeight="130">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="📋 操作日志" FontSize="13" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding OperationLogs}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" FontSize="12"
                                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- 右侧：编辑面板 -->
            <Border Grid.Column="1"
                    Style="{StaticResource CardBorderStyle}"
                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" FontSize="16" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAddingNew}" Value="True">
                                        <Setter Property="Text" Value="➕ 新增用户"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsAddingNew}" Value="False">
                                        <Setter Property="Text" Value="✏️ 编辑用户"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <StackPanel Grid.Row="1" Margin="0,0,0,10">
                        <TextBlock Text="用户名" Style="{StaticResource FormLabelStyle}"/>
                        <TextBox Text="{Binding EditUserName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 IsEnabled="{Binding IsAddingNew}"
                                 Margin="0,4,0,12"/>

                        <TextBlock Text="显示名称" Style="{StaticResource FormLabelStyle}"/>
                        <TextBox Text="{Binding EditDisplayName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 Margin="0,4,0,12"/>

                        <TextBlock Text="用户角色" Style="{StaticResource FormLabelStyle}"/>
                        <StackPanel Margin="0,4,0,12">
                            <RadioButton Content="操作员  (查看监控、启停设备)"
                                         IsChecked="{Binding EditRoleOperator}"
                                         Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4"/>
                            <RadioButton Content="工程师  (调整参数、配置硬件)"
                                         IsChecked="{Binding EditRoleEngineer}"
                                         Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4"/>
                            <RadioButton Content="管理员  (全部权限含用户管理)"
                                         IsChecked="{Binding EditRoleAdmin}"
                                         Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4"/>
                        </StackPanel>

                        <TextBlock Text="账号状态" Style="{StaticResource FormLabelStyle}"/>
                        <CheckBox Content="启用账号" IsChecked="{Binding EditIsEnabled}"
                                  Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,12"/>

                        <TextBlock Text="备注" Style="{StaticResource FormLabelStyle}"/>
                        <TextBox Text="{Binding EditRemark, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                                 VerticalContentAlignment="Top" Margin="0,4,0,12"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="✅ 保存"
                                Command="{Binding SaveEditCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Width="90" Margin="0,0,8,0"/>
                        <Button Content="取消"
                                Command="{Binding CancelEditCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Width="90"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

