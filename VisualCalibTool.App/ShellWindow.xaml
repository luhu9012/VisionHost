<Window x:Class="VisualCalibTool.App.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:VisualCalibTool.Views;assembly=VisualCalibTool.Wpf"
        Title="VisualCalibTool · 可视化标定工具"
        Height="840" Width="1400"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="#FF1B1B1B">
    <!--
      整个独立程序就这一行内容：类库的入口控件。
      嵌入模式下宿主同样只需 new 这个控件，因此两种交付形态跑的是同一份逻辑、同一套 UI。
    -->
    <views:CalibToolHostControl />
</Window>
