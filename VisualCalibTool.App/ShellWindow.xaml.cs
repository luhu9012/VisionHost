using System.Windows;

namespace VisualCalibTool.App
{
    /// <summary>独立模式外壳窗口。内容全部来自类库的入口控件。</summary>
    public partial class ShellWindow : Window
    {
        public ShellWindow()
        {
            InitializeComponent();
        }
    }
}
